package example

object extraCode {
    
    /* Reading and Parsing a JSON file
                        val fileStream = getClass.getResourceAsStream("/pokeDoc1.json")
                                                                            ^this file must be in a resources folder withing the main folder
                        val lines = Source.fromInputStream(fileStream).getLines()

                        lines.foreach(println)

                        "C:/Users/18284/Desktop/Training Program/Files/pokeDoc1.json"
    */                  
}
